/**
 * App Light Theme
 */
import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
   palette: {
      primary: {
         main: '#2E7D4B'
      },
      secondary: {
         main: '#6A737E'
      }
   }
});

export default theme;